<UserControl x:Class="EvChargerUI.Views.PriceChangeLogView.PriceChangeLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:EvChargerUI.Commons.Controls"
             mc:Ignorable="d"
             d:DesignHeight="440" d:DesignWidth="728">
    <UserControl.Resources>
        <Style x:Key="AdminButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="#1BA1C8" CornerRadius="4">
                            <controls:LetterSpacingTextBlock FontFamily="{StaticResource PretendardBoldFont}" FontSize="14" LineHeight="20" Foreground="#FFFFFF" LetterSpacing="-0.2" Text="{TemplateBinding Content}" VerticalAlignment="Center" HorizontalAlignment="Center" TextAlignment="Center" Width="170" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PriceTabUnselectedButtonStyle" TargetType="Button" BasedOn="{StaticResource AdminButtonStyle}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="#E8EEF3" CornerRadius="4">
                            <controls:LetterSpacingTextBlock FontFamily="{StaticResource PretendardBoldFont}" FontSize="14" LineHeight="20" Foreground="#475263" LetterSpacing="-0.2" Text="{TemplateBinding Content}" VerticalAlignment="Center" HorizontalAlignment="Center" TextAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#D8E2EC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#FFFFFF">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="16,12,16,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="단가 스케줄 확인" Command="{Binding ShowPriceScheduleCommand}" Style="{StaticResource PriceTabUnselectedButtonStyle}" Height="44" Padding="12,0"/>
            <Border Grid.Column="2" Background="#1BA1C8" CornerRadius="4" Padding="12,12" VerticalAlignment="Stretch">
                <controls:LetterSpacingTextBlock Text="변경 이력" FontFamily="{StaticResource PretendardBoldFont}" FontSize="15" LineHeight="22" Foreground="#FFFFFF" LetterSpacing="-0.2" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center"/>
            </Border>
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="16,0,16,8">
            <TextBlock Text="{Binding StationInfoText}" FontSize="13" Foreground="#475263" Margin="0,0,0,2"/>
            <TextBlock Text="{Binding DbPathText}" FontSize="12" Foreground="#64748B" TextTrimming="CharacterEllipsis"/>
        </StackPanel>

        <DataGrid Grid.Row="2"
                  Margin="16,0,16,10"
                  ItemsSource="{Binding ChangeLogs}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  ScrollViewer.VerticalScrollBarVisibility="Visible"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  ScrollViewer.CanContentScroll="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="All"
                  RowHeaderWidth="0"
                  SelectionMode="Single"
                  SelectionUnit="FullRow">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="변경일시" Binding="{Binding ChangedAt}" Width="150"/>
                <DataGridTextColumn Header="시간대" Binding="{Binding HourText}" Width="70"/>
                <DataGridTextColumn Header="이전 단가" Binding="{Binding OldPriceText}" Width="90"/>
                <DataGridTextColumn Header="변경 단가" Binding="{Binding NewPriceText}" Width="90"/>
                <DataGridTextColumn Header="변경폭" Binding="{Binding PriceChangeText}" Width="90"/>
                <DataGridTextColumn Header="변경원인" Binding="{Binding ChangeSourceText}" Width="90"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,16,12">
            <Button Content="새로고침" Command="{Binding ReloadCommand}" Style="{StaticResource AdminButtonStyle}" Height="36" Margin="0,0,8,0"/>
            <Button Content="뒤로" Command="{Binding BackCommand}" Style="{StaticResource AdminButtonStyle}" Height="36"/>
        </StackPanel>
    </Grid>
</UserControl>
