<UserControl x:Class="EvChargerUI.Views.Popup.ReservationDescriptionPopupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EvChargerUI.Views.Popup"
             xmlns:controls="clr-namespace:EvChargerUI.Commons.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="345" d:DesignWidth="360">
    <Grid>
        <Border Grid.Column="0"  Background="#FFFFFF" BorderThickness="0" CornerRadius="12"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="26*"/>
                <ColumnDefinition Width="308*"/>
                <ColumnDefinition Width="26*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="28*"/>
                <RowDefinition Height="28*"/>
                <RowDefinition Height="12*"/>
                <RowDefinition Height="112*"/>
                <RowDefinition Height="90*"/>
                <RowDefinition Height="49*"/>
                <RowDefinition Height="26*"/>
            </Grid.RowDefinitions>

            <Viewbox Grid.Row="1" Grid.Column="1">
                <controls:LetterSpacingTextBlock Style="{StaticResource  SmalPopupTitleStyle}"  Text="예약하기란?"/>
            </Viewbox>
            <Viewbox Grid.Row="3" Grid.Column="1">
                <controls:LetterSpacingTextBlock Style="{StaticResource  SmalPopupContentStyle}">
                    현재 충전기가 사용 중일 경우
                    <LineBreak/>
                    '예약하기'를 눌러 휴대폰 번호를 등록,
                    <LineBreak/>
                    충전이 끝난 뒤 자동으로 순서가 안내되어
                    <LineBreak/>
                    편리하게 충전할 수 있는 기능입니다.
                </controls:LetterSpacingTextBlock>
            </Viewbox>
            <Button Grid.Row="5" Grid.Column="1"  Style="{StaticResource ConfirmPopupButtonStyle}"  Command="{Binding CommonClosePopupCommand}" />
        </Grid>
    </Grid>
</UserControl>
