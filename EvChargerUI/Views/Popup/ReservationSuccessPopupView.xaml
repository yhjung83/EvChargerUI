<UserControl x:Class="EvChargerUI.Views.Popup.ReservationSuccessPopupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EvChargerUI.Views.Popup"
             xmlns:controls="clr-namespace:EvChargerUI.Commons.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="230" d:DesignWidth="360">
    <Grid>
        <Border Grid.Column="0"  Background="#FFFFFF" BorderThickness="0" CornerRadius="12"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="26*"/>
                <ColumnDefinition Width="7*"/>
                <ColumnDefinition Width="294*"/>
                <ColumnDefinition Width="7*"/>
                <ColumnDefinition Width="26*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="28*"/>
                <RowDefinition Height="28*"/>
                <RowDefinition Height="84*"/>
                <RowDefinition Height="15*"/>
                <RowDefinition Height="49*"/>
                <RowDefinition Height="26*"/>
            </Grid.RowDefinitions>

            <Viewbox Grid.Row="1" Grid.Column="2">
                <controls:LetterSpacingTextBlock Style="{StaticResource  SmalPopupTitleStyle}"  Text="예약이 완료되었습니다."/>
            </Viewbox>
            <Viewbox Grid.Row="2" Grid.Column="2">
                <controls:LetterSpacingTextBlock Style="{StaticResource  SmalPopupContentStyle}">
                    충전 가능 시 문자로 안내 드리겠습니다.
                    <LineBreak/>
                    문자 수신 후 5분 이내 미 사용시
                    <LineBreak/>
                    예약이 자동 취소됩니다.
                </controls:LetterSpacingTextBlock>
            </Viewbox>
            <Button Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="3" Style="{StaticResource ConfirmPopupButtonStyle}" Command="{Binding CloseCommonPopupCommand}"/>
        </Grid>
    </Grid>
</UserControl>
