<UserControl x:Class="EvChargerUI.Views.Popup.ChargingEndConfirmPopupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EvChargerUI.Views.Popup" xmlns:controls="clr-namespace:EvChargerUI.Commons.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="230" d:DesignWidth="360">
    <Grid>
        <Border Grid.Column="0"  Background="#FFFFFF" BorderThickness="0" CornerRadius="12"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="26*"/>
                <ColumnDefinition Width="7*"/>
                <ColumnDefinition Width="294*"/>
                <ColumnDefinition Width="7*"/>
                <ColumnDefinition Width="26*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="28*"/>
                <RowDefinition Height="28*"/>
                <RowDefinition Height="99*"/>
                <RowDefinition Height="49*"/>
                <RowDefinition Height="26*"/>
            </Grid.RowDefinitions>

            <Viewbox Grid.Row="1" Grid.Column="2">
                <controls:LetterSpacingTextBlock Style="{StaticResource SmalPopupTitleStyle}" Text="충전 종료 확인"/>
            </Viewbox>

            <Viewbox Grid.Row="2" Grid.Column="2">
                <controls:LetterSpacingTextBlock Style="{StaticResource SmalPopupContentStyle}">
                    충전이 아직 완료되지 않았습니다.
                    <LineBreak/>
                    그래도 충전을 종료하시겠습니까?
                </controls:LetterSpacingTextBlock>
            </Viewbox>
            
            <Grid Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Command="{Binding ContinueChargingCommand}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Grid>
                                            <Border x:Name="ButtonBorder"
                                            Background="#F3F4F6"
                                            BorderThickness="1"
                                            CornerRadius="6"
                                            />
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="14*"/>
                                                    <RowDefinition Height="22*"/>
                                                    <RowDefinition Height="13*"/>
                                                </Grid.RowDefinitions>
                                                <Viewbox Grid.Row="1" HorizontalAlignment="Center">
                                                    <controls:LetterSpacingTextBlock Text="계속 충전하기"
                                                             Style="{StaticResource CancelPopupTextStyle}"/>
                                                </Viewbox>
                                            </Grid>
                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
                <Button Grid.Column="2" Style="{StaticResource ConfirmPopupButtonStyle}" Command="{Binding ConfirmEndChargingCommand}"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>

