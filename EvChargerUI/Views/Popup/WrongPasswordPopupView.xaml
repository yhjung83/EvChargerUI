<UserControl x:Class="EvChargerUI.Views.Popup.WrongPasswordPopupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EvChargerUI.Views.Popup"
             xmlns:controls="clr-namespace:EvChargerUI.Commons.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="230" d:DesignWidth="360"
             Focusable="True"
             Loaded="UserControl_Loaded"
             PreviewKeyDown="UserControl_PreviewKeyDown">
    <Grid>
        <Border Grid.Column="0"  Background="#FFFFFF" BorderThickness="0" CornerRadius="12"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="26*"/>
                <ColumnDefinition Width="7*"/>
                <ColumnDefinition Width="294*"/>
                <ColumnDefinition Width="7*"/>
                <ColumnDefinition Width="26*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="28*"/>
                <RowDefinition Height="56*"/>
                <RowDefinition Height="71*"/>
                <RowDefinition Height="49*"/>
                <RowDefinition Height="26*"/>
            </Grid.RowDefinitions>

            <Viewbox Grid.Row="1" Grid.Column="2">
                <controls:LetterSpacingTextBlock Style="{StaticResource  SmalPopupContentStyle}">
                    비밀번호가 일치하지 않습니다.
                    <LineBreak/>
                    다시 입력해 주세요.
                </controls:LetterSpacingTextBlock>
            </Viewbox>
            <Button Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="3" Style="{StaticResource ConfirmPopupButtonStyle}" Command="{Binding ReopenPasswordInputPopupCommand}"/>
        </Grid>
    </Grid>
</UserControl>

