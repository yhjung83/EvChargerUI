<UserControl x:Class="EvChargerUI.Views.Popup.PaymentSuccessPopupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EvChargerUI.Views.Popup"
             xmlns:controls="clr-namespace:EvChargerUI.Commons.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="230" d:DesignWidth="360">
    <Grid>
        <Border Grid.Column="0"  Background="#FFFFFF" BorderThickness="0" CornerRadius="12"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="33*"/>
                <ColumnDefinition Width="50*"/>
                <ColumnDefinition Width="185*"/>
                <ColumnDefinition Width="50*"/>
                <ColumnDefinition Width="32*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="28*"/>
                <RowDefinition Height="28*"/>
                <RowDefinition Height="12*"/>
                <RowDefinition Height="56*"/>
                <RowDefinition Height="31*"/>
                <RowDefinition Height="49*"/>
                <RowDefinition Height="26*"/>
            </Grid.RowDefinitions>

            <Viewbox Grid.Row="1" Grid.Column="2">
                <controls:LetterSpacingTextBlock Style="{StaticResource  SmalPopupTitleStyle}"  Text="결제 완료"/>
            </Viewbox>
            <Viewbox Grid.Row="3" Grid.Column="2">
                <controls:LetterSpacingTextBlock Style="{StaticResource  SmalPopupContentStyle}">
                    카드를 리더기에서
                    <LineBreak/>
                    제거해 주세요.
                </controls:LetterSpacingTextBlock>
            </Viewbox>
            <Button Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="3" Style="{StaticResource ConfirmPopupButtonStyle}" Command="{Binding StartChargingCommand}"/>
        </Grid>
    </Grid>
</UserControl>
