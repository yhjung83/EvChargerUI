<UserControl x:Class="EvChargerUI.Views.LogView.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:EvChargerUI.Commons.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1366">
    <UserControl.Resources>
        <Style x:Key="AdminButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="#1BA1C8" CornerRadius="4">
                            <controls:LetterSpacingTextBlock FontFamily="{StaticResource PretendardBoldFont}" FontSize="14" LineHeight="20" Foreground="#FFFFFF" LetterSpacing="-0.2" Text="{TemplateBinding Content}" VerticalAlignment="Center" HorizontalAlignment="Center" TextAlignment="Center" Width="170" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="진단 뷰어" FontSize="30" FontWeight="Bold" HorizontalAlignment="Center" Margin="10"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- App Log Display -->
            <Grid Grid.Column="0" Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="App Status" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Visible">
                    <TextBox Text="{Binding AppLogContent}" IsReadOnly="True" 
                             TextWrapping="NoWrap" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" 
                             Background="#F0F0F0" BorderBrush="#CCCCCC" BorderThickness="1" Padding="5"/>
                </ScrollViewer>
            </Grid>

            <!-- DSP Log Display -->
            <Grid Grid.Column="1" Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="DSP Status" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Visible">
                    <TextBox Text="{Binding DspLogContent}" IsReadOnly="True" 
                             TextWrapping="NoWrap" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" 
                             Background="#F0F0F0" BorderBrush="#CCCCCC" BorderThickness="1" Padding="5"/>
                </ScrollViewer>
            </Grid>
        </Grid>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="APP 상태 확인" Command="{Binding ShowAppLogCommand}" Style="{StaticResource AdminButtonStyle}" Margin="5"/>
            <Button Content="DSP 상태 확인" Command="{Binding ShowDspLogCommand}" Style="{StaticResource AdminButtonStyle}" Margin="5"/>
            <Button Content="화면 지우기" Command="{Binding ClearLogsCommand}" Style="{StaticResource AdminButtonStyle}" Margin="5"/>
            <Button Content="뒤로" Command="{Binding BackCommand}" Style="{StaticResource AdminButtonStyle}" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>