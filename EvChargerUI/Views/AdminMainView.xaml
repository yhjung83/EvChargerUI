<UserControl x:Class="EvChargerUI.Views.AdminMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EvChargerUI.Views"
             xmlns:controls="clr-namespace:EvChargerUI.Commons.Controls"
             xmlns:converters="clr-namespace:EvChargerUI.Commons.Converters"
             mc:Ignorable="d" 
             Height="378" Width="728">
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid Background="#F3F4F8">
        <Grid.RowDefinitions>
            <RowDefinition Height="90"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="156"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="108"/>
        </Grid.RowDefinitions>
        <Grid Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="358"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="175"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="175"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="39"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="43"/>
            </Grid.RowDefinitions>
            <Button Grid.Row="0" Grid.RowSpan="3" Grid.Column="0" Background="Transparent" BorderThickness="0" Command="{Binding RunMainUiCommand}">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="#1BA1C8" CornerRadius="4">
                            <controls:LetterSpacingTextBlock FontFamily="{StaticResource PretendardBoldFont}" FontSize="23" LineHeight="28" Foreground="#FFFFFF" LetterSpacing="-0.2" Text="UI 프로그램 실행" VerticalAlignment="Center" HorizontalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Grid.Row="0" Grid.Column="2" Background="Transparent" BorderThickness="0" Command="{Binding RestartChargerCommand}">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="#FFFFFF" CornerRadius="4" BorderThickness="1" BorderBrush="#DEE2E7">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Image Source="/Images/ic_restart.png" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                <controls:LetterSpacingTextBlock FontFamily="{StaticResource PretendardFont}" FontSize="14" LineHeight="20" Foreground="#64748B" LetterSpacing="-0.2" Text="충전기 재시작" VerticalAlignment="Center" HorizontalAlignment="Center" />
                            </StackPanel>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Grid.Row="0" Grid.Column="4" Background="Transparent" BorderThickness="0" Command="{Binding RebootOsCommand}">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="#FFFF" CornerRadius="4" BorderThickness="1" BorderBrush="#DEE2E7">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Image Source="/Images/ic_reboot.png" Width="16" Height="16" VerticalAlignment="Center"  Margin="0,0,4,0"/>
                                <controls:LetterSpacingTextBlock FontFamily="{StaticResource PretendardFont}" FontSize="14" LineHeight="20" Foreground="#64748B" LetterSpacing="-0.2" Text="Windows 재부팅" VerticalAlignment="Center" HorizontalAlignment="Center" />
                            </StackPanel>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Grid.Row="2" Grid.Column="2" Background="Transparent" BorderThickness="0" Command="{Binding ChangeToSettingViewCommand}">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="#64748B" CornerRadius="4">
                            <controls:LetterSpacingTextBlock FontFamily="{StaticResource PretendardBoldFont}" FontSize="15" LineHeight="22" Foreground="#FFFFFF" LetterSpacing="-0.2" Text="설정" VerticalAlignment="Center" HorizontalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Grid Grid.Row="2" Grid.Column="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="83"/>
                    <ColumnDefinition Width="9"/>
                    <ColumnDefinition Width="83"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Background="Transparent" BorderThickness="0" Command="{Binding CheckPriceScheduleCommand}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="#64748B" CornerRadius="4">
                                <controls:LetterSpacingTextBlock FontFamily="{StaticResource PretendardBoldFont}" FontSize="14" LineHeight="20" Foreground="#FFFFFF" LetterSpacing="-0.2" Text="단가 스케줄" VerticalAlignment="Center" HorizontalAlignment="Center" />
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button Grid.Column="2" Background="Transparent" BorderThickness="0" Command="{Binding OpenLogViewCommand}">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="#64748B" CornerRadius="4">
                            <controls:LetterSpacingTextBlock FontFamily="{StaticResource PretendardBoldFont}" FontSize="14" LineHeight="20" Foreground="#FFFFFF" LetterSpacing="-0.2" Text="자가진단" VerticalAlignment="Center" HorizontalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Button.Template>
                </Button>
            </Grid>
        </Grid>
        <Grid Grid.Row="2" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="10,0,0,112">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Ellipse Width="10" Height="10" Fill="{Binding ServerStatusColor}" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBlock Text="서버 통신" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Ellipse Width="10" Height="10" Fill="{Binding PaymentDeviceStatusColor}" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBlock Text="단말기 통신" VerticalAlignment="Center"/>
            </StackPanel>            
           
            <StackPanel Grid.Column="4" Orientation="Horizontal">
                <Ellipse Width="10" Height="10" Fill="{Binding PaymentDeviceStatusColor}" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBlock Text="RF 통신" VerticalAlignment="Center"/>
            </StackPanel>
            
        </Grid>

        <!-- Overlay for Diagnosis -->
        <Grid Grid.RowSpan="5" Background="#B3FFFFFF" Visibility="{Binding IsDiagnosing, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="False" Value="{Binding LoadingProgress}" Minimum="0" Maximum="100" Width="200" Height="20" Margin="0,0,0,10"/>
                <TextBlock Text="{Binding DiagnosingMessage}" FontSize="24" FontWeight="Bold" Foreground="#1BA1C8" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
