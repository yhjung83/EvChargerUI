<UserControl x:Class="EvChargerUI.Views.DualChannel.ChargerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:EvChargerUI.Commons.Controls"
             xmlns:local="clr-namespace:EvChargerUI.Views"
             xmlns:enum="clr-namespace:EvChargerUI.Commons.Enum"
             mc:Ignorable="d" 
             d:DesignHeight="412" d:DesignWidth="348">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="36*"></RowDefinition>
            <RowDefinition Height="16*"></RowDefinition>
            <RowDefinition Height="360*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="36*"></ColumnDefinition>
                <ColumnDefinition Width="12*"></ColumnDefinition>
                <ColumnDefinition Width="300*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Command="{Binding HomeInitializeChargerCommand}"  IsEnabled="{Binding IsHomeButtonEnabled}">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource ImageButtonStyle}">
                        <Setter Property="Content">
                            <Setter.Value>
                                <Image Source="/Images/btn_home.png"/>
                            </Setter.Value>
                        </Setter>

                        <Style.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Content">
                                    <Setter.Value>
                                        <Image Source="/Images/btn_home_disabled.png"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Border Grid.Column="2" Background="#33FFFFFF" CornerRadius="{Binding ActualHeight, RelativeSource={RelativeSource Self}, Converter={StaticResource HalfHeightConverter}}"></Border>
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="11*"></RowDefinition>
                    <RowDefinition Height="14*"></RowDefinition>
                    <RowDefinition Height="11*"></RowDefinition>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="25*"></ColumnDefinition>
                    <ColumnDefinition Width="42*"></ColumnDefinition>
                    <ColumnDefinition Width="5*"></ColumnDefinition>
                    <ColumnDefinition Width="14*"></ColumnDefinition>
                    <ColumnDefinition Width="5*"></ColumnDefinition>
                    <ColumnDefinition Width="42*"></ColumnDefinition>
                    <ColumnDefinition Width="5*"></ColumnDefinition>
                    <ColumnDefinition Width="14*"></ColumnDefinition>
                    <ColumnDefinition Width="5*"></ColumnDefinition>
                    <ColumnDefinition Width="52*"></ColumnDefinition>
                    <ColumnDefinition Width="5*"></ColumnDefinition>
                    <ColumnDefinition Width="14*"></ColumnDefinition>
                    <ColumnDefinition Width="5*"></ColumnDefinition>
                    <ColumnDefinition Width="42*"></ColumnDefinition>
                    <ColumnDefinition Width="25*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Image Grid.Row="1" Grid.Column="3" Source="{Binding CurrentChargeSequence, Converter={StaticResource ChargeSequenceToArrowImageConverter}, ConverterParameter=SelectConnector}"/>
                <Image Grid.Row="1" Grid.Column="7" Source="{Binding CurrentChargeSequence, Converter={StaticResource ChargeSequenceToArrowImageConverter}, ConverterParameter=SelectPaymentMethod}"/>
                <Image Grid.Row="1" Grid.Column="11" Source="{Binding CurrentChargeSequence, Converter={StaticResource ChargeSequenceToArrowImageConverter}, ConverterParameter=PlugConnector}"/>

                <Viewbox Grid.Row="1"  Grid.Column="1" >
                    <controls:LetterSpacingTextBlock Text="충전방식" IsEnabled="{Binding CurrentChargeSequence, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=SelectConnector}">
                        <controls:LetterSpacingTextBlock.Style>
                            <Style TargetType="controls:LetterSpacingTextBlock" BasedOn="{StaticResource TopProgressStyle}">
                                <Style.Triggers>
                                    <DataTrigger Value="True">
                                        <DataTrigger.Binding>
                                            <Binding Path="CurrentChargeSequence" Converter="{StaticResource EnumEqualsConverter}" ConverterParameter="SelectConnector"/>
                                        </DataTrigger.Binding>
                                        <Setter Property="TextDecorations" Value="Underline"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </controls:LetterSpacingTextBlock.Style>
                    </controls:LetterSpacingTextBlock>
                </Viewbox>
                <Viewbox Grid.Row="1" Grid.Column="5" >
                    <controls:LetterSpacingTextBlock Text="결제방식" IsEnabled="{Binding CurrentChargeSequence, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=SelectPaymentMethod}">
                        <controls:LetterSpacingTextBlock.Style>
                            <Style TargetType="controls:LetterSpacingTextBlock" BasedOn="{StaticResource TopProgressStyle}">
                                <Style.Triggers>
                                    <DataTrigger Value="True">
                                        <DataTrigger.Binding>
                                            <Binding Path="CurrentChargeSequence" Converter="{StaticResource EnumEqualsConverter}" ConverterParameter="SelectPaymentMethod"/>
                                        </DataTrigger.Binding>
                                        <Setter Property="TextDecorations" Value="Underline"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </controls:LetterSpacingTextBlock.Style>
                    </controls:LetterSpacingTextBlock>
                </Viewbox>
                <Viewbox Grid.Row="1" Grid.Column="9">
                    <controls:LetterSpacingTextBlock Text="커넥터연결" IsEnabled="{Binding CurrentChargeSequence, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=PlugConnector}">
                        <controls:LetterSpacingTextBlock.Style>
                            <Style TargetType="controls:LetterSpacingTextBlock" BasedOn="{StaticResource TopProgressStyle}">
                                <Style.Triggers>
                                    <DataTrigger Value="True">
                                        <DataTrigger.Binding>
                                            <Binding Path="CurrentChargeSequence" Converter="{StaticResource EnumEqualsConverter}" ConverterParameter="PlugConnector"/>
                                        </DataTrigger.Binding>
                                        <Setter Property="TextDecorations" Value="Underline"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </controls:LetterSpacingTextBlock.Style>
                    </controls:LetterSpacingTextBlock>
                </Viewbox>
                <Viewbox Grid.Row="1" Grid.Column="13">
                    <controls:LetterSpacingTextBlock Text="충전상태" IsEnabled="{Binding CurrentChargeSequence, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=Charging}">
                        <controls:LetterSpacingTextBlock.Style>
                            <Style TargetType="controls:LetterSpacingTextBlock" BasedOn="{StaticResource TopProgressStyle}">
                                <Style.Triggers>
                                    <DataTrigger Value="True">
                                        <DataTrigger.Binding>
                                            <Binding Path="CurrentChargeSequence" Converter="{StaticResource EnumEqualsConverter}" ConverterParameter="Charging"/>
                                        </DataTrigger.Binding>
                                        <Setter Property="TextDecorations" Value="Underline"/>
                                    </DataTrigger>
                                    <DataTrigger Value="True">
                                        <DataTrigger.Binding>
                                            <Binding Path="CurrentChargeSequence" Converter="{StaticResource EnumEqualsConverter}" ConverterParameter="Completed"/>
                                        </DataTrigger.Binding>
                                        <Setter Property="TextDecorations" Value="Underline"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </controls:LetterSpacingTextBlock.Style>
                    </controls:LetterSpacingTextBlock>
                </Viewbox>
            </Grid>
        </Grid>
        <Grid Grid.Row="2">
            <ContentControl Content="{Binding CurrentView}" />
            <!-- 반투명 오버레이: 다른 쪽이 결제/커넥터 선택 중일 때 -->
            <Grid Visibility="{Binding IsWaitingOtherSide, Converter={StaticResource BoolToVisibilityConverter}}" 
                  Background="#CC2E2E2E" 
                  Panel.ZIndex="10">
                <ContentControl Content="{Binding WaitingOtherSideView}" />
            </Grid>
        </Grid>
    </Grid>
</UserControl>
